# Builds the module for libjpeg.
!include libjpeg.yaml
!include rcic-module.yaml
---
- package: libjpeg module 
  category: LIBRARIES
  module:
    prepend_path:
      - *Prepend_path
      - *Flagspath
    prereq:
      - *ModuleCompiler_prereq
    requires:
      - *ModuleCompiler_requires
